[General Function]"	画面表示　[Screen]	画面体裁・レイアウト[Screen appearance / layout]	文字サイズ／フォント／色／罫線／ウィンドウサイズ／スクロールバー／印刷時の体裁 [font size / font / color / guideline / window size / scroll bar / printing appearance]
		参照表示値 [Source value]	DB値／セッション値 [DB values / Session Values]
		メッセージ [Messages]	条件／文言 [Conditions / Messages]
		表示制御 [Screen control]	表示・非表示 [display / non-display]
		表示フォーマット　[Display format]	日付／金額／丸め込み表示（...省略）／個別定義のフォーマット [date / monetary / rounding ( … omitted) / individual format ]
		HTML属性 [HTML attribute]	タイトル／画像alt属性／ボタン有効・無効／文字コード／デフォルトカーソル位置／Tabオーダー [title/ image ALT attribution / enable or disable button / character code]
		"最大値・最小値／最大桁数・最小桁数表示 
[Maximum, Minimun/ Max digit number, Minimum digit number]"	
		言語切替 [Language Switch]	
	画面操作制御(js)　[Screen Operation Control (JavaScripts)]		デフォルトカーソル位置／Tabオーダー／伏字入力／Enterキー制御／コピー&ペースト防止／日本語入力・IME制御／ポップアップ制御／ボタン・リンク押下時処理 [default cursor position / tab order / hidden under asterisks / Enter Key Control / Copy & Paste prevention / Japanes Language Input & IME Control / Popup Control / button & Link Processing]
	ブラウザ操作（正常） [Browser Operation (Normal)]	リロード／進む／戻る [Reload / Forward / Back]	
		お気に入りからのダイレクトアクセス [Direct Access from My Favorites]	
	画面遷移 [Screen Transition]	正常画面遷移 [Normal Screen Transition]	
		エラー画面 [Error Screen]	システムエラー／認証・権限エラー／セッションエラー／トークンエラー・404エラー [System error / Authentification & role Error / Session Error / Token Error / 404 Error]
	入力値検証 [Input Value Validation]	型桁必須 [Type, Size, Not null]	
		論理制約 [Logical Constraint]	
		数値範囲 [numerical value range]	
		文字種 [String Type]	文字範囲／改行／スペース／Tab／制御文字 [Avaliable Type / linefeed code / Space / Tab / control code]
		日付（閏日） [Date(leap day)]	
		極端な値 [extreme values]	
		フォーマット（郵便番号／電話番号／URL／メールアドレスなど） [format (Zipcode/Telephone Number/URL/ email Address etc.)]	
	データ登録／参照／更新／削除 [Data CRUD]	登録／更新／削除のDB [Create / Update / Delete ]	
		論理削除制御 [Logical Delete Control]	
		存在確認／ユニーク制約 [Existance check/ Unique Constraint]	
		データ参照時のソート順 [Sort Order in data reference]
		登録／更新／参照時のデータ加工 [Data Processing in Create/Update/Read]	文字列置換（半角カナ→全角カナ変換、半角スペース→全角スペース変換等） [String replace (from hankaku-kana to zenkaku-kana, form hankaku-space to zenkaku-space)]
			削除（禁止文字や半角スペースの削除等） [Trim (Eliminating Prohibited characters or Hankaku-Spaces)]
			編集（日付、氏名、メールアドレスなどの項目結合／分割等） [Update (concatenating / splitting items, such as date, name, email address)]
		nullの取扱い（null->0変換、null->""変換） [Null Porcessing]	
		暗号化／複合化 [Encrypt / Decrypt]	
		限界値（最大値を超える／最小値を下回る場合のエラー制御） [Limit Value (Error Handling on exceeding the Maximum and the Minimum limit)]	
		データ更新の画面反映 [Screen Reflection by Data Update]	
		同一データに対する複数個所からの更新・参照  [Update & Read the same resource from multi functions]	
		検索 [Search]	0件表示 [ Zero record Display ]
			ページ切り替え（前へ／次へ／ページ番号指定） [Switch pages ( back, forward, indicating the page number)]
			存在しなくなったページへのアクセス [access control to the deleted pages]
			重複レコードが存在しないか [Without Duplicated Records]
		シーケンス番号（ビジネスキー）の採番 [Sequence (Business Key) Numbering]	
		トランザクション範囲／ロールバック／同時登録／排他処理／ロック処理の統一性 [Transaction range / Rollback / Simultaneous Input / Exclusive Control / Unity of Lock Process  ]	
		INDEX作成／DB必須制約 [Index / DB Not Null Contraints]	
	アクセス制御 [Access Control]	認可（パーミッション）処理（機能／表示項目） [Permission processing]	ユーザに対する認可 [Permission for Users]
			ロールに対する認可 [Permission for Roles]
		認証（ログイン）処理（トライ回数／ロック／ロック解除／二重ログイン禁止） [Authentification Processing (max retrying / Lock /  Unlock / Block double login)]	
		ロール等による処理／遷移先の切り替え [Switching Processing & Transition by roles]	
		未認証状態での認証を要する画面へのダイレクトアクセス [Direct Access the limit pages in Unauthentification]	
		アクセス可能期間制御（キャンペーン等） [Accessible Expiration Control (Campaigns etc)]	
		ブラウザ（UA）・デバイス・キャリアに応じたリソース切替 [Switching Resources accorting to Browers(UA), Devices, Carrier etc]	
		同一ユーザでのマルチウィンドウ操作 [Multi-Browser operations by a single user]	
		Cookie無効状態でのアクセス [Access in Cookie Invalid]	
		ログアウトしない状態での他サイトとの行き来（spモード決済／3Dセキュア等も含む） [Transions over Multi-sites without logout (sp mode payment / 3D secure)]	
[Specific Function]"	メール送信 [Sending Mails]	形式（プレーンテキスト／HTML） [Format (plain text / HTML)]	
		文字コード [Character Code]	
		ヘッダ／件名／本文の内容 [Header / Subject / Body]	
		文字化け（半角カタカナ、携帯メール） [Garbled email (Hankaku-Katakana, featurephones email)]	
		デリミタのエスケープ処理 [ecape of delimiters]	
		To／CC／BCC／Reply-To	
		リトライ／送信エラー通知 [Retry / Send Error Notification]	
		極端な値を入力した場合 [including extreme values]	
		限界値（宛先件数、一処理当たりの最大送信件数など） [limit value (Number of To., Maximum Sending Number]	
		URLリンクの遷移先 [Transition of URL Links]	
		添付ファイル（ファイル数、容量、ファイル破損） [Attachment (number of files, file size, corrupted file)]	
		到達確認 [arrival confirmation]	
		迷惑メール処理されていないか [spam processing]	
		一括処理（配信成功件数／未達件数／配信中件数など） [processing in one action (success / unreach / in process)]	
	ファイルアップロード・ダウンロード [File Upload, File Download]	ファイル形式（テキスト／画像／MIMEタイプ） [File Format (text/images/MIME type)]	
		ファイル名（長さ／大文字小文字／拡張子） [File Name (length/Upper case, Lower Case/ extension)]	
		文字コード（Shift_JIS／UTF-8） [Character Code (Shift_JIS/UTF-8)]	
		カラーモード（Gray／RGB／CMYK）／解像度（200dpi／100dpi）／画像ビット数（8bit／16bit）[Color Mode(Gray/RGB/CMYK)/resolution(200dpi/100dpi)/bit number (8bit/16bit)]	
		桁ずれ／文字化け／出力順／改行コード／デリミタ／デザイン崩れ／制御コードの混入 [digit shift / garbled character / output order / linefeed code / delimiter / design collapse / contamination of cotrol code]	
		アップロード時のサーバ側でのファイル作成（作成場所／存在チェック／ファイルサイズ制限／MIMEタイプ／圧縮／暗号化／複合化／0バイト／ファイル破損） [Uploading & Creating Files on the server (path, existance, file size restriction, MIME type, compress, encrypt, decrypt, 0 bite, Corrupted Files) ]	
		同名ファイルの上書き／退避　[Overwrite or Backup the file with the same name]	
		単一／複数ファイルのアップロード／ダウンロード [Upload & Download the single-files / multi-files]	
		複数同時アップロード時の書庫化（LZH等） [Archiving the multi-files when uploading them simultaneously]	
		ダウンロードファイルの仕様（ファイルオープン可否／MIMEタイプ／解凍／暗号化／複合化／0バイトファイル／ファイル破損）  [Download files (Open files / MIME type / extract / Encrypt, Decrypt / 0 byte file / corrupted files)]	
		EPSのレイアウト崩れ [garbled layouts of EPS]	
		ロールバック／エラー通知 [Rollback / Error Notification]	
		極端な値（最大桁数／最小値／最大値） [extreme values (the max digits / the minimum value / the maximum value)]	
		限界値（最大ファイル数／最大件数／最大ページ数／最大桁数／最大ファイルサイズ／0件処理） [Limit values (max file number / max records / max pages / max digits / max file size / 0 counts)]	
		ファイル格納先（ディレクトリ） [storage destination folder]	
		連続実行 [sequential excecution]	
		中途の強制終了 [terminating the process]	中断後処理 [processing after terminating]
			中断後の再アップロード／ダウンロード [re-upload / re-download after terminating]
	システム間ファイル連携 [File Transfer between Systems]	形式（テキスト／画像等） [format (text / images etc.)]	
		ファイル名（長さ／大文字小文字／拡張子） [File Name (length/Upper case, Lower Case/ extension)]	
		カラーモード（Gray／RGB／CMYK）／解像度（200dpi／100dpi）／画像ビット数（8bit／16bit）　[Color Mode(Gray/RGB/CMYK)/resolution(200dpi/100dpi)/bit number (8bit/16bit)]	
		桁ずれ／文字化け／出力順／改行コード／デリミタ／デザイン崩れ／制御コードの混入 [digit shift/garbled character/output order/linefeed code/delimiter/design collapse/contamination of cotrol code]	
		"ファイル読込時のチェック（ファイルオープン／ヘッダ／フッタ有無／ファイル数／項目数）／件数チェック／データチェック（重複データ／項目） [validation when the file is read (file open / header / footer / number of files / number of items) / number evaluation / data (duplication records / items) evaluation ]
"	
		ファイル出力時のチェック（作成場所／存在チェック／ファイルサイズ制限／分割／圧縮／暗号化／複合化／0バイトファイル／ファイル破損） [Evaluation in outputing files (path, existance, file size restriction, splited, compress, encrypt, decrypt, 0 bite file,  corrupted file)]	
		対象のファイル、フォルダのみ作成、削除しているか [target files, crateing folders only, deleting folders only]	
		複数ファイルの同時読込／出力 [Simultaneous reading / writing multiple files]	
		IFファイルの仕様 [Specification of the Interface Files]	
		文字コード（Shift_JIS／UTF-8） [Character Code (Shift_JIS/UTF-8)]	
		EPSのレイアウト崩れ [garbled layouts of EPS]	
		ロールバック／エラー通知 [Rollback / Error Notification]	
		極端な値（最大桁数／最小値／最大値／ファイルサイズ） [extreme values (max digits / the minimum value / the maximum values / the max files size )]	
		限界値（最大ファイル数／最大件数／最大桁数／最大ファイルサイズ／0件処理） [Limit Value (the maximum number of files / the maximum number of items / the maximum number of digits / the maximum file size / value of "zero" )]	
		同一ファイルへのアクセス（排他制御／競合時のエラー制御） [Simultaneous Access ( exclusive control, conflict error handling)]	
		連続実行 [sequential excecution]	
		中途の強制終了 [terminating the process]	
	帳票出力 [Form Output]	プレビュー／印字／プリンタ設定 [preview, printing, printer setting]	
		繰り返し項目の出力件数（0件／1件／N件）に応じた出力順／改ページ制御 [Order of printing and Control page (0 / 1 / N) breaks according to the number of repeat items outputs ]	
		再出力 [re-output]	
		極端な値（最大桁数／最小値／最大値） [extreme values (the max digits / the minimum value / the maximum value)]	
		限界値（最大ファイル数／最大件数／最大ページ数／最大桁数／最大ファイルサイズ／0件処理） [Limit values (max file number / max records / max pages / max digits / max file size / 0 counts)]	
		大量データ投入時の処理速度、負荷 [Performance & loads with populating the massive data]	
		テンプレート情報（設定ファイル）が存在しない場合の処理（取り消し）[Processing without Template Information (Configulation files) (Cancel)]	
		レイアウト [Layout]	
		出力項目（フィールド長／改行／文字サイズ／文字化け／出力順／フォント／色／罫線／ヘッダ／フッタ／用紙サイズ／ページ数／改ページ／文字列置換／カーニング） [Output Item (field length, linefeed code, garbled characters, output order, font, color, guideline, header, footer, size of paper, number of pages, page break, string replacement, kerning)]	
	画像生成 [Image Generation]	ファイルフォーマット、領域サイズ、ファイル容量 [file format, area size, file size]	
		バーコードの読み取り [ Reading bar codes]	
	演算・集計処理　[Culcuration & Aggregation]	入力、設定パタンに応じた出力結果 [output results accoring the input values or the configuration patterns]	
		再実行 [redorun]	
		月末日の計算（にしむくさむらい）[the last day of a month (a month with less than 31 days)]	
		オーバーフロー／アンダーフロー／丸め誤差 [overflow / underflow / rounding error]	
	バッチ処理 [Batch Process]	処理結果 [Processing Result]	
		実行タイミング（日時、先行／後続タスク） [Execution Timing (dairy, predecessor / successor tasks, )]	
		リカバリ／再実行 [recovery / redo]	
		連続実行 [sequential excecution]	
		多重実行（同一バッチの複数起動）[multipul execution (multi-execution of the same batch process)]	
		並行実行（複数バッチ／オンライン処理の並行起動）[parallel executions (multi-batch process, booting the parallel execusions of online processings)]	
		ロールバック／エラー通知　[Rollback / Error Notification]	
		トランザクション範囲／排他処理 [Transaction range / exclusive Control]	
		他システムへのファイル転送 [File Transfer to the other system]	
		中途の強制終了　[terminating the process]	
	外部連携　[External Transfer]	疎通確認 [Connectivity Check]	
		異常時リカバリ [Recovery in trouble]	
		自システムステータス制御 [Status Control of the target system]	
	異常時動作 [Behavior in Abnormality]	セッションタイムアウト [Session Timeout]	エラー画面 [Error Screen]
			遷移先 [Transition]
			セッション破棄 [Destroying session]
		異常操作 [Abnormal Operation]	登録／削除繰り返し [Creating & Deleting repeatedly]
		DB接続エラー／SQLエラー [DB Connection Error / SQL Error]	
		インターネット接続エラー [Internet Connection Error]	
		システムエラー時のセッション破棄 [Destorying session in system errors]	
		エラーログ／発報 [error log /notification]	
		エラーメール送信（宛先／内容／タイミング）[Sending the error mail (To, Data, Timing) ]	
		予期しないエラー発生時の応急処理（ユーザ向けエラー画面表示／ログイン画面への転送など） [Preparation for unexpected errors (Error screen for users, Jump to the login Screen)]	
	設定値　[Setting Values]	参照ファイルの指定 [indicating the reference file]	
		設定値変更の反映 [Reflection by Updating the values]	
	日付制御　 [Date Control]	定期実行／日時駆動バッチの動作 [behavior of routine or dairy batch processes]	
		特定日付の動作切り替え [change the behavior on the particular date]	
	マスタデータ変更時の動作 [Behavior in changing master data]	マスタテーブル変更時の動作 [Behavior when the master tables are updated]	
		マスタファイル（コンテンツ等）変更／差替え時の動作 [Behavior when the master files are updated / replaced]	
	ログ出力 [Log Output]	アプリケーションログ [Application log]	ファイル名／ファイルサイズ（ローテーション）／出力場所 [file name / file size (rotation) / path
			出力フォーマット／文字コード／内容／文字化け／制御文字 [Format / character code /　Garbled character / control character] 
			システムエラー等強制終了時のログ出力 [ logging after terminations such as system error ]
		システムログ [Syslog]	ファイル名／ファイルサイズ（ローテーション）／出力場所 [file name / file size (rotation)]
			出力フォーマット／文字コード／内容／文字化け／制御文字 [Format / character code /　Garbled character / control character] 
			システムエラー等強制終了時のログ出力 [ logging after terminations such as system error ]
	外部デバイス [External Devices]	ICカードリーダ／ICタグリーダーの連続リード／連続稼働 [IC Card Reader/ Sequencial Reading of IC Tag Reader / Continuous Operation]	
"非機能
[Nonfanction]"	シナリオ [Senario]	業務との整合性（正常・異常）[Consistency with Business (Normal / Abnormal)]	
		日付・時刻横断 [Date / Time]	
	セキュリティ [Security]	SSL [SSL]	
		サニタイズ [Sanitization]	
		機密情報を公開ディレクトリに作成していないか（ファイルアップロード） [not to create files with the secret informations in public spaces (file upload)]	
		ログインからログアウトまでのセッション維持、ログアウト後のセッション破棄 [Keeping session continuity form the login to the logout, Destroying session after the logout]	
		CGIパラメータ操作（システムエラーにせずエラーメッセージを表示する）	
	移行テスト [Migration Test]	新旧コード値の整合性 [Consitency of new's and old's codes]	
	性能・耐久 [Performance, Endurance]	レスポンスタイム [response times]	
		バッチ実行時間 [batch runtimes]	
		大量データ投入（検索、バッチ処理等への影響） [Populating the massive data (influence on search & batch processes)]	
		大量メール配信の処理速度 [runtimes of massive email distribution]	
	ユーザビリティ [Usability]	ボタン位置、遷移先、文言統一 [Button Position, Transition, Consistency of Words]	
		複数解像度（1024x768、600x800、640x480）での文字潰れ確認 [Evaluation of "glued" letters in one and more resolutions(1024x768、600x800、640x480)]	
	アドホック [ad hoc]	リロード／進む／戻る／登録ボタン連打　[Reload / forward / Back / Submit Button-bashing ]	
		登録／削除繰り返し [create & delete routine]	
	環境別（ＯＳ／ブラウザ） [Environment (OS/Browser)]		
	インストール [Installation]	バージョン [version]	
	監視 [Monitoring]	監視対象 [Monitoring Target]	
		監視アプリ起動 [Booting Monitoring Application ]	
		監視ログ（フォーマット／内容／出力先） [Monitoring log(format/message/target)]	
